import React from 'react';
import { Box } from '@material-ui/core';

const TopNavigationTitle = () => {
  return (
    <Box fontSize={20} flexGrow={1} textAlign="left" color="text">
      <h1>My Shopping List</h1>
    </Box>
  )
}

export default TopNavigationTitle